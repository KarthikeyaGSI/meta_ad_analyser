{
  "name": "ad-automation-root",
  "private": true,
  "scripts": {
    "vercel-build": "npm --prefix frontend install && npm --prefix frontend run build"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "next": "^15.0.0-rc.1"
  },
  "devDependencies": {
    "next": "^15.0.0-rc.1"
  }
}

